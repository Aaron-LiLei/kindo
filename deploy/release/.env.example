# 童映 Kindo 部署环境模板：复制为 .env 后按需修改（全部有默认，MEDIA_DIR 必填）
# 版本：镜像 tag（与 GitHub Release 同名，如 v0.1.0；latest=最新发行）
KINDO_VERSION=latest

# 必填：家庭媒体目录（只读挂载进容器；来源与扫描在 Web 后台录入）
# Windows Docker Desktop 写正斜杠，如 MEDIA_DIR=C:/Users/you/media
MEDIA_DIR=/volume1/media/kids

# Hub 对外端口（默认 8090）
KINDO_PORT=8090
TZ=Asia/Shanghai

# 首次初始化管理员；不设则自动生成一次性 token（见启动日志 data/bootstrap/）
# KINDO_ADMIN_BOOTSTRAP_TOKEN=my-bootstrap-token

# LLM Provider（推荐改用 Web 后台「AI 模型」页录入，密钥落盘加密；
# 若走此环境变量方式，需同步解开 config/kindo.yaml 中 llm_providers 段的注释）
# KINDO_LLM_BASE_URL=https://llm.example.com/v1
# KINDO_LLM_API_KEY=sk-xxx
# KINDO_LLM_MODEL=qwen3.7-max

# 模型包地址覆盖（默认官方 GitHub 模型资产；内网镜像/离线时改此地址）
# ASR_MODEL_URL=http://nas.local:8080/kindo-asr-paraformer-chuan-v1.tar.gz
# TTS_MODEL_URL=http://nas.local:8080/kindo-tts-zipvoice-distill-v1.tar.gz
